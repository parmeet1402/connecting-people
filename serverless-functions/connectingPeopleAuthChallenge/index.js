const { randomDigits } = require("crypto-secure-random-digit");

const mainFunction = () => {};

exports.handler = async event => {
  return mainFunction();
};

// TODO: Comment this function call on deployment
console.log(mainFunction());
